import React from 'react';
import { Typography, Box } from '@mui/material';

const AllExamsPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">All Exams Page</Typography>
      <Typography>Browse all available exams and categories.</Typography>
      {/* TODO: Implement exam listing and filtering */}
    </Box>
  );
};

export default AllExamsPage;
